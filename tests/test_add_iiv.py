from pharmpy.modeling import read_model, add_iiv


#def test_add_iiv(model_path):
#    model = read_model(model_path)
#    first_symbol = model.statements[0].symbol.name
#    model_new = add_iiv(model, first_symbol, 'exp')
#    assert model.code != model_new.code
